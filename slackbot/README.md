# slackbot

## VS Code — AutoBot Patcher

The **AutoBot Patcher** extension lives in [`vscode-autobot/`](vscode-autobot/README.md). Open that folder in VS Code, run `npm install && npm run compile`, then F5 to debug.
